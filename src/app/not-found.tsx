'use client'

import { Template } from "@/components";

export default function NotfoundPage(){
    return (
        <Template>
            <section style={{ marginTop: '200px' }}>
                <h1 className="text-gray-900 text-center" style={{ fontSize: '56px' }}>Page Not Found!</h1>
            </section>
        </Template>
    )
}
